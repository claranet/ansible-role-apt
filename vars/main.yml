---

_aptconfig_: "{{ aptconfig_default | combine(aptconfig, recursive=True) }}"

_apt_python: python{{ "3" if ansible_python.version.major == 3 else "" }}

_apt_required_packages:
  - "{{ _apt_python }}-apt"
  - apt-transport-https
  - aptitude
  - "{{ _apt_python }}-pycurl"
  - gpg
